# Dashboard Core Dependencies (Required)
streamlit==1.29.0
plotly==5.18.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
networkx>=3.0.0
requests>=2.31.0
openpyxl>=3.1.0

# Note: Machine Learning dependencies are not required for running the dashboard.
# They are only needed if you want to regenerate the classification files.
# To install ML dependencies, use: pip install -r requirements-ml.txt
